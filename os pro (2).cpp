#include<iostream>
using namespace std;
int main()
{
	int BT,PR;
	//cout<<"the names of process are"<<A<<B<<C<<D<<E<<endl;//
	cout<<"the arrival time for all processes are 0 second";
	int BTa,PRa;
	int BTb,PRb;
	int BTc,PRc;
 	int BTd,PRd;
 	int BTe,PRe;
	cout<<"enter the burst times for all proceses"<<endl;
	cout<<"the burst time and priority of process A is:"<<endl;
	cin>>BTa>>PRa;
	cout<<"the burst time and priority of process B is:"<<endl;
	cin>>BTb>>PRb;
	cout<<"the burst time and priority of process C is:"<<endl;
	cin>>BTc>>PRc;
	cout<<"the burst time and priority of process D is:"<<endl;
	cin>>BTd>>PRd;
	cout<<"the burst time and priority of process E is:"<<endl;
	cin>>BTe>>PRe;
	if((PRa<PRb) && (PRa<PRc) && (PRa<PRd) && (PRa<PRe))
		{
		cout<<"Process A will execute for 10 seconds"<<endl;
	
	
	if((PRb<PRc) && (PRb<PRd) && (PRb<PRe))
	{
		cout<<"Process B will execute for 6 seconds"<<endl;
	
	
	if((PRc<PRd) && (PRc<PRe))
	{
		cout<<"Process C will execute for 2 seconds"<<endl;
	
	if(PRd<PRe)

	{
		cout<<"Process D will execute for 1 seconds"<<endl;
	
	{
		cout<<"Process E will execute for 4 seconds"<<endl;
	
		
	
	
	
	
	 if((PRb<PRa) && (PRb<PRc) && (PRb<PRd) && (PRb<PRe))
	{
		cout<<"Process B will execute for 6 seconds"<<endl;
		
		if((PRa<PRc) && (PRa<PRd) && (PRa<PRe))
		{
			cout<<"Process A will execute for 10 seconds"<<endl;
			if((PRc<PRd) && (PRc<PRe))
			{
				cout<<"Process C will execute for 2 seconds"<<endl;
			if(PRd<PRe)
			{
				
			cout<<"Process D will execute for 1 seconds"<<endl;
	
	{
		cout<<"Process E will execute for 4 seconds"<<endl;
	
	
	if((PRc<PRa) && (PRc<PRb) && (PRc<PRd) && (PRc<PRe))
	{
		cout<<"Process C will execute for 2 seconds"<<endl;
		if((PRa<PRb) && (PRa<PRd) && (PRa<PRe))
		{
			cout<<"Process A will execute for 10 seconds"<<endl;
			if((PRb<PRd) && (PRb<PRe))
			{
				cout<<"Process B will execute for 6 seconds"<<endl;
			
			if(PRd<PRe)
			{
				cout<<"Process D will execute for 1 seconds"<<endl;
	
	{
		cout<<"Process E will execute for 4 seconds"<<endl;
		
	
 if((PRd<PRa) && (PRd<PRb) && (PRd<PRc) && (PRd<PRe))
	{
		cout<<"Process D will execute for 1 seconds"<<endl;
		if((PRa<PRb) && (PRa<PRc) && (PRa<PRe))
		{
			cout<<"Process A will execute for 10 seconds"<<endl;
			if((PRb<PRc) && (PRb<PRe))
			{
				
				cout<<"Process B will execute for 6 seconds"<<endl;
				if(PRc<PRe)
				{
					cout<<"Process C will execute for 2 seconds"<<endl;
					{
						cout<<"Process E will execute for 4 seconds"<<endl;
					
	
	if((PRe<PRa) && (PRe<PRc) && (PRe<PRd) && (PRe<PRb))
	{
		cout<<"Process E will execute for 4 seconds"<<endl;
	 	if((PRa<PRb) && (PRa<PRc) && (PRa<PRd))
	 	{
	 			cout<<"Process A will execute for 10 seconds"<<endl;
	 			if((PRb<PRc) && (PRb<PRd))
	 			{
	 				cout<<"Process B will execute for 6 seconds"<<endl;
	 				if(PRc<PRd)
	 				{
	 					cout<<"Process C will execute for 2 seconds"<<endl;
	 					{
	 						cout<<"Process D will execute for 1 seconds"<<endl;
						 }
					 }
				 }
		 }
	}
}
}
}

}
}
}
}
}
}
}
}
}
}
}
}
}
}
}
}
}

	
	return 0;
	
	
	
	
	
}
